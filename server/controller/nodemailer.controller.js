const nodemailer = require("nodemailer");

module.exports = {
    sendMail: async(req,res)=>{
        let config = {
            service:"gmail",
            auth:{
                user:"",
                pass:""
            }
        }
    }
}